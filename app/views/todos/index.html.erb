<div class="container">
  <h1>Current TODO list</h1>

  <table>
    <tr>
      <th class="description"><h4>Description</h3></th>
      <th class="days_old"><h4>Days Old</h4></th>
      <th class="completed"></th>
    </tr>
    <% @todos.each do |t|%>
      <tr>
        <td class="description"><%= t.description %></td>
        <td class="days_old"><%= t.age_of_todo %></td>
        <td class="completed"><%= button_to 'Completed', {:id => t.id, :action => 'destroy'}, :method => :delete, data: {confirm: 'Do you want to delete this Todo?'} %></td>
      </tr>
    <% end %>
    <tr>
      <td class="description">
        <%= form_for @todo do |form| %>
          <%= form.label 'Add New' %>
          <br />
          <%= form.text_field :description, placeholder: 'Description', size: 60 %></td>
      <td class='days_old'>
        <h4>0</h4></td>
      <td class='completed'>
          <%= form.submit 'Save' %></td>
      <% end %>
    </tr>
  </table>
</div>

